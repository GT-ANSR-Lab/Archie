from sys.archie import *

# hardwareVar = Hardware("name", "device type (supported types are DEVICE_TYPE.COMPUTE, DEVICE_TYPE.LINK, DEVICE_TYPE.ROUTER, DEVICE_TYPE.STORAGE")
# hardwareVar.set_entire_configuration({ "config": "value"})
